let spotifySong = `--------------------------------------------------------------------
          Artist(s): ${song.artists[0].name} 
          Song Name: ${song.name}
          Album Name:  ${song.album.name} 
          Preview Link:  ${song.preview_url}`;
